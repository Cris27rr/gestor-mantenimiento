{
  "name": "gestor-mantenimiento",
  "private": true,
  "scripts": {
    "dev": "cd web && bun run dev",
    "build": "cd web && bun run build",
    "start": "cd web && bun run preview",
    "serve": "cd web && bun run build && bun run preview",
    "lint": "cd web && bun run lint",
    "test": "cd web && bun run test"
  }
}
